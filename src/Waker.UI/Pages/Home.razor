@page "/"
@using Waker.Aplication.Controllers
@inject SoundController SoundController

﻿<AddTimerButton />
<Button OnClick="Test">Show</Button>
<ModalWindow IsVisible=_isVisible></ModalWindow>

@code {
    private bool _isVisible;

    protected override async Task OnInitializedAsync()
    {
        // 動作確認用
        var ents = await SoundController.GetEntities();
    }

    private async Task Test()
    {
        _isVisible = true;
    }
}
